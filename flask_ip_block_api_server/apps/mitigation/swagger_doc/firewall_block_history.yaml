tags:
  - '[미티게이션 페이지] firewall_block_history api'
parameters:
  - in: query
    name: category
    type: string
    required: true

  - in: query
    name: keyword
    type: string
    required: false
    allowEmptyValue: true

  - in: query
    name: limit
    type: integer
    required: true

  - in: query
    name: offset
    type: integer
    required: true

responses:
  200:
    description: 미티게이션 페이지 블락 히스토리 리스트
    schema:
      id: firewall_block_history
    examples:
      {
        "status": 200,
        "result": "Your request is successful",
        "message": "Request fulfilled, document follows",
      }
  400:
    description: 미티게이션 페이지 블락 히스토리 리스트 api 실패
    schema:
      id: firewall_block_history failed
    examples:
      {
        "status": 400,
        "result": "Your request is failed",
        "message": "Bad request syntax or unsupported method",
      }
